{
  "check": {
    "id": "docker-registry",
    "name": "docker-registry",
    "port": 80,
    "script": "echo 'Checking HTTP endpoint of registry (curl -sI localhost|grep 200)'; curl -sI localhost|grep 200",
    "interval": "10s"
  }
}
